# PostgreSQL config for Rails Performance Lab
# Used to enable pg_stat_statements and tuning (overridden by command in docker-compose)

shared_preload_libraries = 'pg_stat_statements'
pg_stat_statements.track = all
shared_buffers = 256MB
work_mem = 16MB
maintenance_work_mem = 128MB
